import 'dart:io';

class FrameExtractor {
  static Future<List<File>> extractFrames(String videoPath, int count) async {
    // Fallback: mengembalikan list kosong karena dependensi belum tersedia
    return [];
  }
}
